# /interpretability/

SHAP (primary) and attention-based (where architecturally available)
explanation extraction code, applied per architecture from `/models/`.

Results include Top-1 / Top-3 attribution accuracy against the ground-truth
driving variable recorded for each injected anomaly in `/data/DATASET.md`.
This is the project's strongest contribution — framed per
`LIMITATIONS.md` Section 4 (item F in `CLAIMS_AUDIT.md`).
