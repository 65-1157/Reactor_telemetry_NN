# /models/results/

Engineering-axis evaluation outputs (Step 10): detection performance
(AUROC/AUPRC), training cost, inference latency, parameter count, and
robustness to missing/irregular data, for all four architectures and the
ML baselines, per the frozen `METRICS.md` protocol.
